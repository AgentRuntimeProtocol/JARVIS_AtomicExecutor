# Copy this file to `.env.local` for local development:
#
#   cp .env.example .env.local
#
# This file is intended for developer convenience only. In production, prefer
# configuring environment variables via your deployment platform and secret store.

# ==============================================================================
# Incoming auth (JWT validation)
# ==============================================================================
#
# This service uses `arp-standard-server` JWT middleware.
# Configuration is driven by `ARP_AUTH_PROFILE` plus optional overrides.
# Explicit `ARP_AUTH_*` env vars override profile defaults.
#
# Profiles:
# - dev-secure-keycloak: JWT required; issuer defaults to http://localhost:8080/realms/arp-dev
# - dev-insecure: JWT disabled (dev only)
# - enterprise: JWT required (bring your own issuer/JWKS)

# --- Default: local Keycloak STS (dev-secure-keycloak) ---
ARP_AUTH_PROFILE=dev-secure-keycloak
ARP_AUTH_ISSUER=http://localhost:8080/realms/arp-dev
ARP_AUTH_AUDIENCE=arp-atomic-executor

# --- Optional overrides (commented out) ---
# ARP_AUTH_MODE=required
# ARP_AUTH_JWKS_URI=
# ARP_AUTH_OIDC_DISCOVERY_URL=
# ARP_AUTH_ALGORITHMS=RS256
# ARP_AUTH_CLOCK_SKEW_SECONDS=60
# ARP_AUTH_EXEMPT_PATHS=/v1/health,/v1/version

# --- Dev-only: disable auth entirely ---
# ARP_AUTH_PROFILE=dev-insecure

# ==============================================================================
# Execution controls (optional)
# ==============================================================================
#
# Coarse per-request timeout for handler execution (seconds).
# JARVIS_DEFAULT_TIMEOUT_SECS=30
#
# Cap the number of concurrent in-flight executions (best-effort).
# JARVIS_MAX_CONCURRENCY=50
